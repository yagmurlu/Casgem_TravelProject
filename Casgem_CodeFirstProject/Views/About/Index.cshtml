
@{
    Layout = null;
}

<!doctype html>
<html lang="en">
@Html.Action("PartialHead","Default")
<body id="home">
    <section class=" w3l-header-4 header-sticky">
        <!--header-->
        @Html.Action("PartialNavbar", "Default")
        <!--/header-->
    </section>
    @Html.Action("PartialSliderScript", "Default")
    <!--theme switcher dark and light mode script//-->
    <!-- breadcrumbs -->
    @Html.Action("PartialSection")
    <!-- breadcrumbs //-->
    @Html.Action("PartialSection1")
    @Html.Action("PartialSection2")
    @Html.Action("PartialSection3")


    @Html.Action("PartialFooter", "Default")

    <!-- move top -->
    @Html.Action("PartialMainScript", "Default")
    <!-- /move top -->

</body>

</html>
